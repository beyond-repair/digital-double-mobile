import React from 'react';
import { PixelWorkspace } from './components/PixelWorkspace';

export const App: React.FC = () => {
  return (
    <div className="app">
      <PixelWorkspace />
    </div>
  );
};
